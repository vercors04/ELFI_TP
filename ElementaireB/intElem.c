#include <stdio.h>
#include "../Utilitaire/utilitaires.h"


void intElem (int t, int nbneel, float** coordElem, float** matelm, float* vecelem){
    float** cofvar = alloctab (2, 2);
    float* x = allocvec (2);
    int q = q_associate(t);
    



    transFK(nbneel, coordElem, )


}